# ─── TradeMirror · env ────────────────────────────────────────────────────
# Copy to .env.local and fill in. The app runs WITHOUT this key (deterministic
# fallback synthesiser), but live Qwen narratives need it.

# Claimed from the TG admin after Qwen-credit approval (first 300 KYC'd teams).
# Gateway: https://hackathon.bitgetops.com/v1 · model: qwen3.8-max
BITGET_QWEN_API_KEY=
